# Batch 15.03 — GunWriter IA rewrite

**Repo:** ArmourShop

STONE_HOE carry/reload + CROSSBOW aim IA items; skins.yml `ia.…`; delete CMD registry/overrides; harness.

**Done when:** harness writes IA yaml + `ia.` skins.yml; no CMD registry.

**Status:** Done — CMD dual-write removed; GunWriter emits IA items + `ia.` skins.yml.
