# Batch 13.04 — Pack: item_3d + helmet_3d + armor helmet branch

**Repo:** ArmourShop

1. `Item3dWriter`, `Helmet3dWriter` (`generate: false` + `model_path`).  
2. `ArmorSetWriter` branch for 3D helmet per tier.  
3. Fixture harness.

**Done when:** harness writes valid IA YAML + models.
