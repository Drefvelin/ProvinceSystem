# Batch 13.06 — Live apply + shop + delete

**Repo:** ArmourShop

1. `PackApplyService` download/write for new kinds + `helmet_3d_tiers`.  
2. `ShopSubmissionWriter` `ps_items` with `set: shields|helmets|…`.  
3. Remover covers new models/textures.

**Done when:** approved → pack + shop + LP for each kind + mixed armor.
