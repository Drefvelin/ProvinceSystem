# `main` pre-editor wipe snapshot

Archived before step **72.10** (Calavorn data prep).

These files are the last committed `duchy.json`, `kingdom.json`, and `empire.json` for map `main` before lore staff rebuild the hierarchy in the web map editor.

- `county.json` was **not** archived here (geometry kept; rename pass via editor).
- Not required in git long-term beyond this reference; operators may keep local copies.
