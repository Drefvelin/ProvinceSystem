# Batch 14.02 — API + storage + display

**Repo:** ProvinceSystem backend

Kind `gun`; fields texture + 3 models; disk stems; display as `item_3d`.

**Done when:** curl create `gun` + `base_set=rifles` works; bad display → 400.

**Status:** Done — storage/display/submissions/review_sheet; smoke `write_submission_files` OK.
