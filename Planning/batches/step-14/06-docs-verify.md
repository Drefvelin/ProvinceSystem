# Batch 14.06 — Docs verify + smoke

- [x] Upload gun → approve → apply → carry/reload/aim poses (pack harness + apply/shop/delete wiring)
- [x] Delete frees CMDs + removes skins.yml key (`GunWriter.remove` + registry)

**Note:** Full in-game staging smoke (aim/reload poses on a live rifle) is operator-run after deploying the ArmourShop jar + config gun paths.

**Future:** GaG IA id support could drop CMD dual-write; not in this step.

**Done when:** checklist green; Step 14 complete.
