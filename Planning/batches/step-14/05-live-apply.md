# Batch 14.05 — Live apply + shop + delete

PackApplyService + ShopSubmissionWriter (`gunskin({id})`) + remover + config paths.

**Done when:** approved gun → shop + GaG apply on staging.

**Status:** Done — apply/shop/delete wired; `config.yml` gun paths set in resources + live plugins copy.
