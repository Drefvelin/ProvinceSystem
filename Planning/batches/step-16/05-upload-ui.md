# Batch 16.05 — Upload UI wire-up

**Repo:** `ProvinceSystem` frontend (`UploadForm` / KindPicker area)

Embed the viewer on `/skins` when enough files are present for the selected kind. Controls for display slot + kind variant. Hide or show 2D fallback for flat-only kinds.

**Depends on:** [02](./02-json-model-render.md)–[04](./04-kind-variants.md) as needed

**Done when:** Picking model+texture (and variants) updates the live preview during an upload session.

**Status:** Planned.
