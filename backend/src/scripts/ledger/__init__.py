"""Economy ledger — SimpleFactions snapshots, partitioned raw + daily canonical.

Named `ledger` on purpose: `chronicle` already means the map timelapse in this
repo and survives here only as the upload mode string the plugin owns.
"""
