{
  "name": "tfmc-sheet-render",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/sync-skins.mjs",
    "build": "esbuild src/browser.ts --bundle --outfile=dist/browser.js --format=iife --global-name=SheetRender --platform=browser",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "playwright": "^1.55.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.3"
  }
}
