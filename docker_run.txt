docker-compose down --volumes --remove-orphans
docker-compose rm -f
docker rmi provincesystem_backend provincesystem_frontend
docker-compose build --no-cache
docker compose up -d

# uvicorn
python -m uvicorn server:app --host 0.0.0.0 --port 8000 --log-level warning