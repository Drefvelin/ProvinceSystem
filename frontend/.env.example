NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
# Lock entire site behind dev landing; staff need character code + tfmc.map.staff
# NEXT_PUBLIC_SITE_DEV_GATE=1
# Local character wizard iteration (skip redeem; load fixture catalog)
# NEXT_PUBLIC_CHARACTER_UI_DEV=1
# Map title editor (staff / UI-dev entry). Off by default until re-enabled.
# NEXT_PUBLIC_MAP_EDITOR_ENABLED=1
